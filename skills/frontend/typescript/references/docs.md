# TypeScript Guidelines (OpenCode Frontend)

- Use strict typing at all times
- Interfaces for object shapes, types for unions/intersections
- Functions should be pure and reusable
- Separate logic, types, and UI
- Use assets templates for new TS components or utilities

<!-- vs code suggestions -->

- Use PascalCase for component names
- Use camelCase for variables and functions
- Use snake_case for file names
- Use kebab-case for CSS classes
- Use `const` for variables that are not reassigned
- Use `let` for variables that are reassigned
- Use `readonly` for variables that are not reassigned
- Use `as` for type assertions
- Use `is` for type guards
- Use `interface` for object shapes
- Use `type` for unions/intersections
- Use `enum` for enums
- Use `namespace` for namespaces
- Use `module` for modules
- Use `export` for exports
- Use `import` for imports
- Use `default` for default exports
- Use `*` for wildcard imports
- Use `as` for type assertions
- Use `is` for type guards
- Use `interface` for object shapes
- Use `type` for unions/intersections
- Use `enum` for enums
- Use `namespace` for namespaces
- Use `module` for modules
- Use `export` for exports
- Use `import` for imports
- Use `default` for default exports
